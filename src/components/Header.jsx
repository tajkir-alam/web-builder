import React from 'react';

const Header = () => {
    return (
        <header className='primary-bg text-white py-[14px] ps-[26px] font-semibold'>
            Web Builder Editor
        </header>
    )
};

export default Header;